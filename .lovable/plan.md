

# Replace Site-Wide Favicon with Rainbow Arc Logo

## Current State
- The Drift landing page logo displays correctly (verified via screenshot)
- The current favicon is `public/favicon.jpeg` referenced in `index.html`

## Change
Copy the uploaded rainbow arc image to `public/favicon.jpeg`, replacing the existing favicon so the browser tab icon matches the new brand identity.

## Steps

1. **Copy the uploaded image** to `public/favicon.jpeg` (overwriting the existing file)
2. No changes needed to `index.html` since it already references `/favicon.jpeg`

### Files modified
- `public/favicon.jpeg` -- replaced with the rainbow arc logo image

